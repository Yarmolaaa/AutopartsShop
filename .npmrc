# redux-form (deprecated, but required by the coursework spec) declares peer deps
# for React 16/17 + react-redux <=7. It works fine against React 18 + react-redux 8,
# so we relax peer resolution rather than downgrade the whole stack.
legacy-peer-deps=true
